extends layout

block content
  h2= `Delete post: ${post.title}`
  if(currentUser.membershipStatus === 'Admin')
    form(action="", method="post") 
      p Are you sure you want to delete this post?
      button Submit
  else
    p You must be an admin to be able to delete posts.
